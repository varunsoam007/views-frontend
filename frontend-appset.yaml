apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet

metadata:
  name: views-frontend
  namespace: argocd

spec:
  generators:
  - list:
      elements:
      - env: dev
      - env: stage
      - env: prod

  template:
    metadata:
      name: 'views-frontend-{{env}}'

      annotations:
        argocd-image-updater.argoproj.io/image-list: frontend=varunsoam/views-frontend
        argocd-image-updater.argoproj.io/write-back-method: git
        argocd-image-updater.argoproj.io/frontend.update-strategy: newest-build

    spec:
      project: default

      source:
        repoURL: https://github.com/varunsoam007/views-frontend.git
        targetRevision: main
        path: .
        helm:
          valueFiles:
            - gitops/{{env}}/values.yaml

      destination:
        server: https://kubernetes.default.svc
        namespace: frontend-{{env}}

      syncPolicy:
        automated:
          prune: true
          selfHeal: true

        syncOptions:
        - CreateNamespace=true