deployments:
  - name: views-frontend
    namespace: frontend-stage
    replicas: 

    containers:
      - name: views-frontend-stage
        image:
          registry: docker.io
          repository: varunsoam/views-frontend
          tag: "343"

services:
  - name: views-frontend
    namespace: frontend-stage
    type: LoadBalancer

    ports:
      - name: http
        port: 80
        targetPort: 80